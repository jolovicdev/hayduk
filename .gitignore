# build output
bin/
ui/dist/
dist/

# the embedded UI stays committed: go:embed all:dist must resolve for go build
# (the negation must come after the blanket dist/ to win)
!internal/server/dist/

# dependencies and tool caches
node_modules/
.vite/
*.tsbuildinfo

# go test binaries
*.test
